// the view
class PostView {
  displayPosts(posts: Post[]) {
    // render posts to html page
  }

  getPostFromInput(): Post {
    // retrieve input values from the HTML page
    // and create a new Post object
    return newPost;
  }
}